<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knockfish.repository.FileReferenceRepository">

    <resultMap id="BaseResultMap" type="com.knockfish.entity.FileReference">
        <id column="file_id" property="fileId"/>
        <result column="reference_id" property="referenceId"/>
        <result column="reference_type" property="referenceType"/>
        <result column="user_id" property="userId"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="mime_type" property="mimeType"/>
        <result column="file_size" property="fileSize"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        file_id, reference_id, reference_type, user_id, file_name, file_path, mime_type, file_size, create_time
    </sql>

    <insert id="insert" parameterType="FileReference" useGeneratedKeys="true" keyProperty="fileId" keyColumn="fileId">
        INSERT INTO file_reference
        (reference_id, reference_type, user_id, file_name, file_path, mime_type, file_size, create_time)
        VALUES (#{referenceId}, #{referenceType}, #{userId}, #{fileName}, #{filePath}, #{mimeType}, #{fileSize}, NOW())
    </insert>

    <delete id="deleteByReference">
        DELETE FROM file_reference
        WHERE reference_type = #{referenceType}
        AND reference_id = #{referenceId}
    </delete>

    <delete id="deleteById">
        DELETE FROM file_reference
        WHERE file_id = #{fileId}
    </delete>

    <delete id="batchDeleteByIds">
        DELETE FROM file_reference
        WHERE file_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="batchUpdateReferenceId" parameterType="map">
        UPDATE file_reference
        SET reference_id = #{referenceId}
        WHERE file_id IN
        <foreach collection="fileIds" item="fileId" open="(" separator="," close=")">
            #{fileId}
        </foreach>
    </update>

    <update id="updateReferenceId">
        update file_reference
        SET reference_id = #{referenceId}
        WHERE file_id = #{fileId}
    </update>

    <update id="unbindByIds">
        UPDATE file_reference
        SET reference_id = NULL
        WHERE file_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="selectByReference" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM file_reference
        WHERE reference_type = #{referenceType}
        AND reference_id = #{referenceId}
        ORDER BY file_id ASC
    </select>

    <select id="selectAllReferencedPaths" resultType="string">
        SELECT DISTINCT file_path
        FROM file_reference
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM file_reference
        WHERE file_id = #{fileId}
    </select>

    <select id="selectAllNoPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM file_reference
        ORDER BY file_id ASC
    </select>

    <select id="selectOrphanFiles" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM file_reference
        WHERE
        (
            reference_id IS NULL
            AND create_time &lt; DATE_SUB(NOW(), INTERVAL 72 HOUR)
        )
        OR
        (
            reference_id IS NOT NULL
            AND reference_type = 'article'
            AND reference_id NOT IN (SELECT article_id FROM article)
        )
        OR
        (
            reference_id IS NOT NULL
            AND reference_type = 'note'
            AND reference_id NOT IN (SELECT note_id FROM note)
        )
    </select>

</mapper>